apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-ddq.fullname" . }}-test-connection"
  labels:
{{ include "example-ddq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "example-ddq.fullname" . }}:80']
  restartPolicy: Never
