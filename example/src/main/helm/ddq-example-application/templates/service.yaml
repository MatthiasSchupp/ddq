apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-ddq.fullname" . }}
  labels:
    {{- include "example-ddq.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-ddq.selectorLabels" . | nindent 4 }}
